spring:
  datasource:
    url: jdbc:h2:mem:payprosys_test;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false

jwt:
  secret: test-secret-key-min-256-bits-for-hs256
  expiration-ms: 3600000
